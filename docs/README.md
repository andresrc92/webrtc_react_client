# Docs

- [`webrtc-streaming-protocol.md`](./webrtc-streaming-protocol.md) — how Isaac Sim's WebRTC streaming server works, the client library this app is built on, and every source consulted while reverse-engineering/confirming it (NVIDIA's protocol isn't publicly spec'd as a standalone document).
- [`isaac-sim-version-support.md`](./isaac-sim-version-support.md) — which Isaac Sim versions this client targets, what was actually verified against a live instance in this session (vs. what's inferred from docs), and a known-issues note.
